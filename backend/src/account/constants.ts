enum OnboardingState {
  COMPLETE = 'complete',
  SCHEDULED = 'scheduled',
  CANCELED = 'canceled',
  UNSCHEDULED = 'unscheduled',
}

enum AccountApplicationState {
  APPROVED = 'approved',
  REJECTED = 'rejected',
  IN_REVIEW = 'in_review',
}

enum AccountRole {
  ADMIN = 'admin',
  STAFF = 'staff',
  CONTRIBUTOR = 'contributor',
}
